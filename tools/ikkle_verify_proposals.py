#!/usr/bin/env python3
"""
Verify both Gemini and ChatGPT proposals with actual numbers.
"""
from collections import Counter

ikkle_raw = [
    ,
]

PLACEHOLDERS = {35,36,37,38,64,127}
PLACEHOLDER_NIBS = (0xE, 0xA, 0xA, 0xE)
TILDE_NIBS = (0x0, 0x6, 0xA, 0x0)

def get_nibs(ch, fix=True):
    if fix and ch in PLACEHOLDERS:
        return PLACEHOLDER_NIBS
    if fix and ch == 126:
        return TILDE_NIBS
    i = ch - 32
    return tuple((ikkle_raw[i*5+s] >> 4) & 0xF for s in range(4))

def nib_art(n):
    return ''.join('#' if (n >> (3-i)) & 1 else '.' for i in range(4))

def half_hamming(a, b):
    d = 0
    for shift in [4, 0]:
        na = (a >> shift) & 0xF
        nb = (b >> shift) & 0xF
        d += bin(na ^ nb).count('1')
    return d

def greedy_reduce(all_vals, target_k):
    freq = Counter(all_vals)
    kept = set(all_vals)
    removals = []
    while len(kept) > target_k:
        best_cost = float('inf')
        best_remove = None
        best_target = None
        for v in sorted(kept):
            others = kept - {v}
            nearest = min(others, key=lambda o: half_hamming(v, o))
            cost = freq[v] * half_hamming(v, nearest)
            if cost < best_cost or (cost == best_cost and freq.get(v,0) < freq.get(best_remove,0)):
                best_cost = cost
                best_remove = v
                best_target = nearest
        kept.remove(best_remove)
        removals.append((best_remove, best_target, best_cost, freq[best_remove]))
    return sorted(kept), removals

# =====================================================================
# GEMINI PROPOSAL 1: Split LUT AFTER fixing placeholders + tilde
# =====================================================================
print("=" * 65)
print("GEMINI PROPOSAL 1: Split LUT k=16 AFTER all odd-nibble fixes")
print("=" * 65)

tops_fixed = []
bots_fixed = []
for ch in range(32, 128):
    n = get_nibs(ch, fix=True)
    tops_fixed.append((n[0] << 4) | n[1])
    bots_fixed.append((n[2] << 4) | n[3])

top_u = sorted(set(tops_fixed))
bot_u = sorted(set(bots_fixed))
print(f"  After fixing placeholders + ~:")
print(f"  Unique tops: {len(top_u)}")
print(f"  Unique bots: {len(bot_u)}")

if len(top_u) <= 16 and len(bot_u) <= 16:
    print("  *** BOTH FIT IN 16 — NO MERGE NEEDED! ***")
else:
    print(f"  Need to merge: tops {len(top_u)}→16, bots {len(bot_u)}→16")
    top_kept, top_rem = greedy_reduce(tops_fixed, 16)
    bot_kept, bot_rem = greedy_reduce(bots_fixed, 16)

    # Build maps and count errors
    top_map = {}
    for v in set(tops_fixed):
        top_map[v] = v if v in top_kept else min(top_kept, key=lambda k: half_hamming(v,k))
    bot_map = {}
    for v in set(bots_fixed):
        bot_map[v] = v if v in bot_kept else min(bot_kept, key=lambda k: half_hamming(v,k))

    top_idx = {v:i for i,v in enumerate(top_kept)}
    bot_idx = {v:i for i,v in enumerate(bot_kept)}

    affected = []
    total_err = 0
    for ch in range(32, 128):
        n = get_nibs(ch, fix=True)
        top_orig = (n[0]<<4)|n[1]
        bot_orig = (n[2]<<4)|n[3]
        top_enc = top_map[top_orig]
        bot_enc = bot_map[bot_orig]
        err = half_hamming(top_orig, top_enc) + half_hamming(bot_orig, bot_enc)
        total_err += err
        if err > 0:
            affected.append((ch, err, top_orig, top_enc, bot_orig, bot_enc))

    print(f"\n  Result: {len(affected)} chars affected, {total_err} total pixel errors")
    print(f"  Data: 128B (16+16+96), Decoder: ~30B, Total: ~158B")

    if affected:
        print(f"\n  Affected characters:")
        for ch, err, to, te, bo, be in affected:
            n_orig = get_nibs(ch, fix=True)
            n_enc = ((te>>4)&0xF, te&0xF, (be>>4)&0xF, be&0xF)
            o_art = ' '.join(nib_art(x) for x in n_orig)
            e_art = ' '.join(nib_art(x) for x in n_enc)
            print(f"    '{chr(ch)}' err={err}  {o_art}  →  {e_art}")

    # Show merged entries
    print(f"\n  Top merges ({len(top_rem)}):")
    for rem, tgt, cost, freq in top_rem:
        print(f"    0x{rem:02X} → 0x{tgt:02X}  freq={freq} cost={cost}")
    print(f"\n  Bot merges ({len(bot_rem)}):")
    for rem, tgt, cost, freq in bot_rem:
        print(f"    0x{rem:02X} → 0x{tgt:02X}  freq={freq} cost={cost}")

# =====================================================================
# GEMINI PROPOSAL 2 / CHATGPT: 3-bit (12-bit) encoding comparison
# =====================================================================
print(f"\n{'=' * 65}")
print("BOTH PROPOSALS: 3-bit encoding cost comparison")
print("=" * 65)

# ChatGPT: 2 chars per 3 bytes (bitstream)
# Gemini: array_hi[96] + array_lo[48] = 144 bytes
# Both: 144 bytes data

# Decoder cost estimates (Z80 bytes):
print("  Data: 144 bytes (both proposals)")
print()
print("  ChatGPT format: 2 chars / 3 bytes (tight bitstream)")
print("    Pro: minimal data size")
print("    Con: cross-byte field extraction for EVERY char")
print("    Decoder: ~55-65 bytes (bit extraction + even/odd branch)")
print("    Total: 144 + 60 = ~204 bytes")
print()
print("  Gemini format: array_hi[96] + array_lo[48]")
print("    Pro: array_hi is direct-indexed (no bit extraction)")
print("    Con: array_lo needs nibble extraction (like our current scheme)")
print("    Con: reconstruction of D,E from split 12-bit value")
print("    Decoder: ~58-65 bytes")
print("    Total: 144 + 62 = ~206 bytes")
print()
print("  Current baseline: nibble-packed 192B + 15B decoder = 207B")
print()
print("  NET SAVINGS:")
print("    ChatGPT 3-bit:  207 - 204 = ~3 bytes")
print("    Gemini hi/lo:   207 - 206 = ~1 byte")
print("    Gemini SplitLUT: 207 - 158 = ~49 bytes (LOSSY)")

# =====================================================================
# COMBINED: What about 3-bit data + simpler decoder?
# =====================================================================
print(f"\n{'=' * 65}")
print("HYBRID: Can we make a cheaper 3-bit decoder?")
print("=" * 65)
print("  The bottleneck is reconstructing D,E (nibble-packed bytes)")
print("  from 12 bits of 3-bit codes.")
print()
print("  Key insight from ChatGPT: code*2 = nibble (just add a,a)")
print("  But the bit EXTRACTION from packed format costs ~40-50B.")
print()
print("  What if we store codes in 4-bit slots (wasting 1 bit/code)?")
print("  Format: same as current nibble-pack but store CODE not nibble")
print("  code = nibble >> 1, decode = code << 1")
print("  Data: 192 bytes (same!), decoder: +4 bytes (4x add a,a)")
print("  Total: 192 + 19 = 211 bytes. WORSE.")
print()
print("  Conclusion: the 3-bit savings (48B) are almost entirely")
print("  eaten by the Z80 extraction overhead (45-50B).")
print("  Net improvement: 0-8 bytes depending on implementation.")
print()
print("  The Split LUT approach (Gemini #1) saves ~49 bytes")
print("  but at cost of visual fidelity. Let's see HOW BAD it really is.")
